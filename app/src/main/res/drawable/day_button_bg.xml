<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 안쪽 색 (연한 회색 정도, 필요하면 바꿔도 됨) -->
    <solid android:color="#FFEFEFEF" />

    <!-- 살짝만 둥근 직사각형 (완전 원 X) -->
    <corners android:radius="1dp" />

    <!-- 테두리 -->
    <stroke
        android:width="1dp"
        android:color="#FF888888" />
</shape>
